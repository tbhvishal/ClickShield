/**
 * Application Configuration
 * Simple port defaults for backend and frontend
 */

export const config = {
  ports: {
    backend: 8001,
    frontend: 5173,
  },
} as const;

export default config;
