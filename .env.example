# ClickShield Environment Variables
# Copy this file to .env and add your actual API key

# Google Safe Browsing API key (required for threat checks)
# Get your key from: https://developers.google.com/safe-browsing/v4/get-started
GOOGLE_SAFE_BROWSING_API_KEY=your_api_key_here

# Backend API URL for frontend (only needed for local development)
#  IMPORTANT: Must match the backend port in config.ts (default: 8001)
# For production/Vercel: leave empty or comment out (will use relative URLs)
VITE_BACKEND_API_URL=http://localhost:8001
